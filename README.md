Sources for the plugin `plugin.gameanalytics`.

Add following to your `build.settings` to use:
```lua
{
    plugins = {
        "plugin.gameanalytics" = {
            publisherId = "com.gameanalytics",
        },
    },
}
```
